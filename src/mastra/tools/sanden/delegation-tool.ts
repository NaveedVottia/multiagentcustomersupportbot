import { createTool } from "@mastra/core/tools";
import { z } from "zod";

// Note: delegateTo tool has been moved to orchestrator-tools.ts
// This file is kept for backward compatibility but the main tool is now in orchestrator-tools

export const delegationTools = {
  // delegateTo is now exported from orchestratorTools
};
